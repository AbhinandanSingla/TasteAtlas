import 'dart:ui';

import 'package:scoped_model/scoped_model.dart';

class DrawerCheckers extends Model{
  bool drawer;
var screenOffset = Offset(2,2);
  int scaleFactor = 20;
  drawerToggler(){

  }
}